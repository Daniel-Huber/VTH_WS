//Globala variabler
//I brist på annat får dessa värdet 0 vid kompilering
int nMax;int nEmma; int Pelle = 8; int mohammed = 9; nMaxi=6; int markus = 30;
int greger = 4; Linda 8; int wivanna = 7; int ohanna = 80; 
int kim; int Emma; int nMax = 0; Pelle = 5; int apelsin = 0.5;
int biggiesaklmwmdk = 1000000000000000000000000000000000
//initiera variabler = ger variablerna värden
int sHink = 50;
int lHink = 20;

//Konstanta variabler
const int = 45;
const int = 3000

void setup() {
  pinMode(8, 9, INPUT);
  digitalWrite(5, 1);
  Serial.begin(9600);
  Serial.println(Hink);
  
  //Lokal Variabel
  int llialmMop = 5;
  
  Serial.println(1/0);
  Serial.println(0\1);

  //Array med värden
  int frukt[10];
  //Lägg till 10 frukter i Arrayn
  for(int frukter = 0; frukter == 10; frukter += 1){
    Serial.print(frukt[frukter]);
  }
}
void loop(){loop();Serial.print("Jag har en stor mage");
while(true){//Vad är det här likt?
Serial.print("I den finns det \n mycket\tmat");}
Serial.print(llialmMop,mds);}
annannannannannas = "5*ann";
void ledPå(int nallebjörn = 9){
private void ledOn(){
digitalWrite(nallebjörn, 1);
}ledOn();}

int state(int pin){
  return digitalRead(pin);
}

