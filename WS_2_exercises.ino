int globVar;
int globVar2;

int skogen = 8;
int lokomotiv = 90;

void setup() {
  int 50;
  pinMode(LED_BUILTIN, OUTPUT);     
  Serial.begin(9600);
  Serial.print(LED_BUILTIN);
  int monno = 9;
  const int lokVar;
  monno = 8 + monno * lokomotiv + skogen;
}

void loop() {
  serial.print("monno: ");
  Serial.println(monno);
  toggle();
  serial.print(lokVar);
  delay(1000)                     
  toggle();  
  serial.print(lokVar + kiwi);
  delay(2000);
  Serial.print(LED_BUILTIN);                     
}

kiwi = 10;

void toggle(){
  Serial.print("not digitalRead(): ");
  Serial.println(!digitalRead(LED_BUILTIN));
  digitalWrite(LED_BUILTIN, !digitalRead(LED_BUILTIN));
}

void lampaPÅ(double pin){
  pinMode(5, OUTPUT);
  digitalWrite(pin, OUTPUT); 
}

void pinModeO(int till, int från){
  int SIST = från;
  while(1 + till > sist){
    pinMode(från, INPUT);
    sist -= 1
  }
}

void kjansdms(int dwqUrsm, double aNsmdwqJk = 4, double PstnAldsoaPmmfoI){
  int något;
  double någotAnnat
  int rekt78fma192mszlm[PstnAldsoaPmmfoI];
  for(något = 9; dwqUrsm <= PstnAldsoaPmmfoI; dwqUrsm -=){
    rekt78fma192mszlm[något] = dwqUrsm
  }
  while(!false){
    Serial.print("Wat!?");
  }
  if(HIGH == 1){
    for(något = 0; något > någotAnnat; något += 5){
      Serial.print(rekt78fma192mszlm[något]);
    }
  }
}


